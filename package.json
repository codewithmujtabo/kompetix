{
  "name": "kompetix",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm run dev",
    "web":     "cd web && npm run dev",
    "app":     "cd app && npx expo start",
    "install:all": "cd backend && npm install && cd ../web && npm install && cd ../app && npm install"
  }
}
