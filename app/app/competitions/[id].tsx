import React from "react";
import { View } from "react-native";

// Archive route: competitions detail moved out of tab layout to avoid creating extra tab entries.
export default function CompetitionDetailArchive() {
  return <View />;
}
